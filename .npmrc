registry=https://registry.npmjs.org/
# always-auth moved to workflow (actions/setup-node). Leaving it commented to avoid npm deprecation warnings.
# always-auth=true
# NOTE: Do NOT put an auth token in source control.
# GitHub Actions will inject an auth token via the Changesets action or setup-node.
