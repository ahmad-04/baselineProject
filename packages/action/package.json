{
  "name": "@baseline-tools/action",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npx tsc -p tsconfig.json",
    "test": "node --test --watch=false",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "@baseline-tools/cli": "file:../cli"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "rimraf": "^5.0.5"
  }
}
