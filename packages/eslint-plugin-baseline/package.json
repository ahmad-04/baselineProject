{
  "name": "@whoisahmad/eslint-plugin-baseline",
  "version": "0.0.1",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "npx tsc -p tsconfig.json",
    "test": "node -e \"process.exit(0)\"",
    "clean": "rimraf dist"
  },
  "peerDependencies": {
    "eslint": ">=8"
  },
  "dependencies": {
    "@whoisahmad/baseline-tools-core": "^0.0.1"
  },
  "devDependencies": {
    "eslint": "^9.9.0",
    "typescript": "^5.5.4",
    "rimraf": "^5.0.5"
  }
}
