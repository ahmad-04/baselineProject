{
  "name": "@baseline-tools/lsp-server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/server.js",
  "types": "dist/server.d.ts",
  "sideEffects": false,
  "description": "Language Server Protocol server for Baseline Guardrails (diagnostics, hovers, code actions)",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@baseline-tools/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "rimraf": "^5.0.5"
  }
}
